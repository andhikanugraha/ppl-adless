var filtersBox = $('#edit-box');var sitesBox = $('#sites-box')$('#save').click(function() {  text = $('#edit-box').val();  self.port.emit('text-entered', text);});$('#defaults').click(function() {  self.port.emit('load-defaults', true);})// Listen for 'show' event to load data.self.port.on("show", function (data) {  filtersBox.val(data.filters);  sitesBox.val(data.sites);});